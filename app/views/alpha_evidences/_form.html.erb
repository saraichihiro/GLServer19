<%= form_for @alpha_evidence, url: alpha_item_alpha_evidence_path(@alpha_item) do |form| %>
  <% if @alpha_evidence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alpha_evidence.errors.count, "error") %> prohibited this alpha_evidence from being saved:</h2>

      <ul>
      <% @alpha_evidence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "根拠" %>
    <%= form.text_area :document, id: :alpha_evidence_document %>
  </div>

  <div class="field">
    <%= form.check_box :completed, id: :alpha_evidence_completed %>
    <%= form.label "完了" %>
  </div>

  <div class="actions">
    <%= form.submit "変更する", class: "btn btn-primary" %>
    <%= link_to 'キャンセル', alpha_state_alpha_items_path(@alpha_state), class: "btn btn-primary"  %>
  </div>
<% end %>
