<%= form_with(model: alpha_state, local: true) do |form| %>
  <% if alpha_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alpha_state.errors.count, "error") %> prohibited this alpha_state from being saved:</h2>

      <ul>
      <% alpha_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :completed %>
    <%= form.check_box :completed, id: :alpha_state_completed %>
  </div>

  <div class="field">
    <%= form.label :completed_items %>
    <%= form.number_field :completed_items, id: :alpha_state_completed_items %>
  </div>

  <div class="field">
    <%= form.label :completed_at %>
    <%= form.datetime_select :completed_at, id: :alpha_state_completed_at %>
  </div>

  <div class="field">
    <%= form.label :alpha_alpha_id %>
    <%= form.text_field :alpha_alpha_id, id: :alpha_state_alpha_alpha_id %>
  </div>

  <div class="field">
    <%= form.label :alpha_state_def_id %>
    <%= form.text_field :alpha_state_def_id, id: :alpha_state_alpha_state_def_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
